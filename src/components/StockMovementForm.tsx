<p className="text-xs text-slate-500 dark:text-slate-400">
              {cost_type === "pacote" 
                ? "Número de unidades dentro do pacote" 
                : "Quantidade a adicionar ao estoque"
              }
            </p>